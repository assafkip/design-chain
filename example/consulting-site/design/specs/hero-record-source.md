# Hero element: Record B, "the source"

`REVIEWED BY FOUNDER:` <!-- date, only when actually reviewed -->

## What it is

The second of two abstract records (RULE-2026-09-18-I/J). What the claim should trace to and
should agree with. Same abstraction rule as Record A: generic, no specific document type.

## Shape

Identical construction to `hero-record-claim.md`: hand-sketched rectangular panel, rough.js, fixed
seed, two interior content lines, no hachure.

## Label

"THE SOURCE" — same treatment as "THE CLAIM": small caps, muted grey (`#8a97ab`), above the panel.

## Position

Right of Record A, same shared baseline (`one-baseline-composition`).

## Motion

`svg-path-draw`, same ease and technique as Record A. Draws second, staggered to start before
Record A's draw fully completes (~70% overlap, matching the ratio `svg-path-draw`'s own guidance
gives for sequential multi-path draws), so the two panels read as one continuous drawing motion,
not two separate animations.

## Open

Same open question as Record A: current spec describes the scratch-verified version's fidelity,
not necessarily final production weight.
