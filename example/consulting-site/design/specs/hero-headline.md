# Hero element: headline

`REVIEWED BY FOUNDER:` <!-- date, only when actually reviewed -->

## What it is

"Automated. But is it true?" — RULE-2026-09-18-I, supersedes RULE-2026-09-16-F's "Your team works
more hours than it bills."

## Source

Picked over two ICP-question-shaped alternates ("It's automated. Is it right?", "It says it's
synced. Is it?") and a pure-declarative alternate ("Automated doesn't mean true."). Combines the
founder's own declarative register (`voice/identity.md`) with the ICP's question-shaped words
(cluster A: "is the automation really working?").

## Type spec

Per `NARRATIVE.md` section 2 ("the cheapest real upgrade available"): 72-96px, weight 400-500,
tracking ≈ -0.05em, three to six words. "Automated. But is it true?" is five words, fits without
adjustment. Centered (`design-chain.json` `standard.hero_align: center`, per the group's majority
move and the founder's repeated correction against left-alignment).

## Caps check, not yet run

`design-chain.json` hero caps: `max_hero_words: 18`, `max_hero_pieces: 2`, `max_hero_lines: 3`.
Headline alone is 5 words, well under. Not yet checked TOGETHER with the sub-line once one exists.

## Open

**The sub-line is undecided.** RULE-2026-09-16-F's old headline had one ("the line under it has no
end period either"); this headline doesn't have a replacement yet. Needs its own pass, grounded in
the same corrected diagnosis and cluster data the headline itself came from, not invented fresh.
