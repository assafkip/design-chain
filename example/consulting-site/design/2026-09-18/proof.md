# Proof — what's on the page, and what kind of proof it is

Step 7: every artifact on every page, its proof kind, and the record it comes from.

## The hero drawing (D1, D2, D3)

**No specific artifact, by design.** RULE-2026-09-18-J keeps Record A and Record B abstract: no
document type, no client, no number. RULE-2026-09-18-B's guardrail (carried onto the hero by
RULE-2026-09-18-C) is explicit: "no client, no engagement, no number, no recreated record" here.
So the hero makes no factual claim requiring a `proof-audit/INDEX.md` citation, it never asserts
that a specific thing happened.

**Proof kind: reliability, demonstrated structurally, not cited.** `design-dna.md` section 4b:
"his systems behave differently from generic AI (the source shown, verdicts from data, stopping
when it cannot prove, misses published next to passes)." The mechanism itself, checking, then
resolving to agree, disagree, or stop rather than guess, IS the demonstration. There is no
underlying record to cite because the claim is about how the system behaves in general, not about
one audited outcome.

## Section 2 (words and links under the hero)

**"Every system I've shipped is on the Work page, including what it got wrong."** Not a proof
artifact itself, a pointer to where proof lives, per RULE-2026-09-18-B item 3: "Proof does not live
here; it lives on the Work pages and the case pages." No citation needed on this page; the claim is
that proof exists elsewhere, which the Work page's own design-chain rounds already carry.

## What this means for this round

No line in `proof-audit/INDEX.md` is cited because none applies. That absence is itself checked:
per `design-dna.md` section 4b, "before any artifact goes on a page, name the kind of proof it is
and the claim it supports." Done here: the hero supports a reliability claim through mechanism, not
through a cited fact, and the guardrail that keeps it abstract is the reason no citation exists, not
an oversight.
